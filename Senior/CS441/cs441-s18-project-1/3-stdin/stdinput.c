/*
 * [YOUR NAME]
 * [DATE LAST MODIFIED]
 *
 * [DESCRIPTION OF THE PROGRAM]
 */
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <ctype.h>

#define MAX_LINELEN 1024

/**
 * Disset the string by counting the types of letters contained within.
 *
 * Parameters:
 *   str: Input string to process
 *   num_space: Updated to reflect the number of spaces (passed-by-reference)
 *   num_digit: Updated to reflect the number of digits (passed-by-reference)
 *   num_lower: Updated to reflect the number of lowercase letters (passed-by-reference)
 *   num_upper: Updated to reflect the number of uppercase letters (passed-by-reference)
 *   num_other: Updated to reflect the number of other characters (passed-by-reference)
 *
 * Returns:
 *   Length of the input string.
 */
int dissect_string(char *str, int *num_space, int *num_digit,
                   int *num_lower, int *num_upper, int *num_other);


int main(int argc, char **argv) {

    return 0;
}
